# Setup Instructions

Create the PostgreSQL database:
CREATE DATABASE mfa_project;

Run the schema file located at:
backend/sql/schema.sql

Install dependencies:
cd backend
npm install

cd ../frontend
npm install

Run the application:

Start backend:
cd backend
npm run dev

Start frontend in a new terminal:
cd frontend
npm start

Open in browser:
http://localhost:3000
